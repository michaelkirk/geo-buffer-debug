{
  "name": "geo-buffer-debug",
  "version": "1.0.0",
  "description": "Interactive debugging tool for geo buffer operations",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "build-wasm": "cd ../wasm && wasm-pack build --target web --out-dir ../web/pkg"
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.8",
    "typescript": "^5.2.2",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  }
}